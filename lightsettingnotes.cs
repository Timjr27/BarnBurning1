using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class lightsettingnotes : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /**
     * Window > render > light > Intensity Multiplier von 1 auf 0 geändert bzw Environment Reflections Source von Skybox auf Custom geändert
     * Window > render > light > Environment Lighting Source von Skybox auf Color geändert
     * 
     */
}
